# Flat node_modules instead of pnpm's symlinked store. Turbopack's CSS
# resolver fails to follow the symlinks on Windows when the project path
# contains spaces, so `@import 'tailwindcss'` in globals.css can't resolve.
node-linker=hoisted
