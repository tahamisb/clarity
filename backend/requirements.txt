fastapi==0.115.5
uvicorn[standard]==0.32.1
google-genai==2.8.0
# EOL SDK — still used by app/services/chat_service.py, which serves the LIVE
# POST /cancellations/chat endpoint (via cancellation_service.chat_with_data).
# Migrate that to google-genai before dropping this package.
google-generativeai==0.8.3
tqdm==4.67.1
python-dotenv==1.0.1
pydantic==2.10.3
pydantic-settings==2.6.1
python-multipart==0.0.18
pytest==8.3.4
pytest-asyncio==0.24.0
# TestClient, used by scripts/warehouse_parity.py.
httpx==0.28.1

# ─── Warehouse ──────────────────────────────────────────────────────────────
# WAREHOUSE_BACKEND=sqlite uses stdlib sqlite3 and needs nothing here.
# WAREHOUSE_BACKEND=postgres reads the simulated live warehouse (warehouse/).
# A floor rather than a pin: the binary wheel has to exist for whatever CPython
# the image brings.
psycopg[binary]>=3.2,<4
psycopg-pool>=3.2,<4

# ─── Cancellation prediction ────────────────────────────────────────────────
# Data lives in data/clarity.db (stdlib sqlite3) — no warehouse client needed.
pandas==2.2.3
numpy==2.1.3
scikit-learn==1.5.2
joblib==1.4.2
